MySQL BackUp

mysqldump --help
man mysqldump 

# Бекап без создания таблиц
mysqldump --all-databases --no-create-info -u root -p > dump-data.sql
; or
mysqldump --all-databases -u root -p > dump-data.sql

nano dump-data.sql

-- <- это однострочный коментарий
/* ... */;
/* ... */; <- это многострочный коментарий

# C сохранением позиции бинлога
mysqldump -h 10.128.15.220 -p --all-databases --events --routines --master-data=2 > dump_file

# Сохранение одной конкретной таблицы, из базы otus
mysqldump outs test_tbl -u root -p > dump-data-otus_test_tbl.sql

==============================
